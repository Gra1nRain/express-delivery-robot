# Debug Map Artifacts

Day 1 debug map outputs should be placed here when they are small enough to
keep in Git:

```text
day1_fast_lio.pcd
map.yaml
map.pgm
semantic_map.yaml
```

Large bags and raw recordings belong in `recordings/day1/` and stay out of Git.
